import SwiftUI // Importing the SwiftUI framework

@main // Declaring the entry point of the application
struct Chapter4_3App: App { // Defining the main application structure
    var body: some Scene { // Declaring the body property of the application
        WindowGroup { // Defining a window group
            SlideshowView() // Displaying the SlideshowView within the window group
        }
    }
}
